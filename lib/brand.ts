// The league's display name, used everywhere the app is titled (tab title,
// sidebar, auth screen, etc.) so it only has to change in one place.
export const APP_NAME = "Ethiosinia Fantasy Premier League";
